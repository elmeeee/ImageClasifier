{
    "buildCommand": "cd frontend && npm ci && npm run build",
    "outputDirectory": "frontend/.vercel/output/static",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.py"
        }
    ]
}