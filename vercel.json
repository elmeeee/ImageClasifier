{
    "buildCommand": "cd frontend && npm ci --legacy-peer-deps && npm run build && cp -r .vercel/output/* ../.vercel/output/ 2>/dev/null || true",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.py"
        }
    ]
}