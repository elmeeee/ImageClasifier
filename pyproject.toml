[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "cifar10-dashboard"
version = "1.0.0"
requires-python = ">=3.10,<3.11"
dependencies = [
    "flask==3.0.0",
    "flask-cors==6.0.0",
    "werkzeug==3.1.5"
]

[tool.setuptools]
py-modules = []
